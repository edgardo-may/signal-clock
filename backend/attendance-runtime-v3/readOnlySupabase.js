'use strict'

class ReadOnlyViolationError extends Error { constructor(message, code = 'ATTENDANCE_RUNTIME_WRITE_DENIED') { super(message); this.name = 'ReadOnlyViolationError'; this.code = code } }
function readOnlyQueryGuard(query, counters) { return new Proxy(query, { get(target, property, receiver) { if (property === 'then') return target.then?.bind(target); if (['insert', 'update', 'delete', 'upsert'].includes(property)) return () => { counters.databaseWrites += 1; throw new ReadOnlyViolationError('Attendance Runtime no permite DML por el cliente de lectura.') }; const value = Reflect.get(target, property, receiver); if (typeof value !== 'function') return value; return (...args) => { const result = value.apply(target, args); return result && typeof result === 'object' ? readOnlyQueryGuard(result, counters) : result } } }) }
function createReadOnlyClient(client, counters) { return new Proxy(client, { get(target, property, receiver) { if (['storage', 'functions', 'auth', 'channel', 'removeChannel', 'removeAllChannels'].includes(property)) return new Proxy({}, { get: () => () => { counters.indirectSupabaseCalls += 1; throw new ReadOnlyViolationError('Attendance Runtime no permite acceso indirecto.', 'ATTENDANCE_RUNTIME_INDIRECT_ACCESS_DENIED') } }); if (property === 'rpc') return () => { counters.rpcWriteCalls += 1; throw new ReadOnlyViolationError('Attendance Runtime no permite RPC por el cliente de lectura.') }; const value = Reflect.get(target, property, receiver); if (typeof value !== 'function') return value; return (...args) => { const result = value.apply(target, args); return property === 'from' && result && typeof result === 'object' ? readOnlyQueryGuard(result, counters) : result } } }) }
module.exports = { ReadOnlyViolationError, createReadOnlyClient }
