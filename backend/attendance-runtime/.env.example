# Server-only: never copy these values to Vite, browser, or frontend configuration.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
ATTENDANCE_RUNTIME_INTERNAL_TOKEN=
ATTENDANCE_RUNTIME_PORT=8088
RUNTIME_VERSION=attendance-runtime-v1
BUILD_SHA=replace-with-immutable-release-sha
